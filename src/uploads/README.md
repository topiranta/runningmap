The server uploads user's GPX files here before converting and merging them to the master GEOJSON file located in src/data.

The GPX files are removed after merging them so this folder should mostly remain empty.
